#ifndef AUTHORIZE_H
#define AUTHORIZE_H

#include <QObject>

class Authorize : public QObject
{
    Q_OBJECT
public:
    explicit Authorize(QObject *parent = nullptr);

signals:

};

#endif // AUTHORIZE_H
